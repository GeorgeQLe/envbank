# EnvBank v0.1.1 launch evidence ship manifest

## User goal

Prove the public v0.1.1 corrective release anonymously, preserve a durable
evidence record, complete Milestone 2, and leave v0.1.0 immutable.

## Changed files and purpose

- `docs/release-evidence-v0.1.1.md`: record publication URLs, tag and commit,
  asset and OCI digests, signer identities, redirect chain, verification
  commands, redacted output, health assertions, and v0.1.0 preservation.
- `scripts/verify-release-anonymous.sh`: replace the optional Buildx manifest
  dependency with GHCR's public bearer-token and OCI manifest APIs.
- `.github/workflows/release.yml`: update upload/download artifact actions to
  pinned Node 24 generations, resolving warnings surfaced by the tagged run.
- `tasks/launch-v0.1.0-ship-manifest.md`: mark v0.1.0 immutable,
  provenance-incomplete, and superseded by v0.1.1.
- `tasks/todo.md`, `tasks/history.md`, and this manifest: complete Milestone 2
  and record the evidence shipping boundary.

## User-goal mapping

The report covers every required anonymous assertion. The verifier passed only
after the portable OCI fix. The release tag, eight assets, provenance bundles,
and versioned image were not modified by the evidence work; v0.1.0 likewise
remains unchanged.

## Tests run

- Complete credential-isolated `./scripts/verify-release-anonymous.sh` against
  the public v0.1.1 release and GHCR image.
- Bash 3.2 parsing, ShellCheck, negative archive fixtures, actionlint, and
  `git diff --check` for the evidence diff.
- The evidence PR must pass the same 11 formatting/module/vet, Linux/macOS Go,
  race, Node 24 extension, recovery, cross-build, Govulncheck, Gitleaks, and
  Go/JavaScript CodeQL checks used by the preparation PR.

## Skipped tests

The application source, protocol, storage, and release artifacts did not change
in this evidence branch. The existing local and tagged release test suites and
the evidence PR's protected checks provide the executable application coverage;
no release rerun is appropriate because v0.1.1 is immutable.

## Adversarial review

The evidence was checked against the public APIs rather than authenticated
connector output. The verifier isolates credentials, rejects unsafe archive
paths/types before extraction, verifies local bundles with Sigstore trusted
roots and exact source policy, uses immutable image identity, avoids deleting
pre-existing images, guards container/volume cleanup by created state, and
records the initially failed portability assertion before the successful rerun.

## Residual risk

Public GitHub, Sigstore/TUF, GHCR, and Docker availability remain external
dependencies for future reruns. The evidence proves the 2026-08-09 snapshot and
the immutable digests recorded in the report; mutable `0.1` is a convenience
channel and deployments should use the recorded digest.

## Rollback note

Revert the evidence squash commit to remove the report, task completion, action
updates, and portable verifier change. Do not delete or rewrite the v0.1.1 or
v0.1.0 tags, releases, assets, provenance bundles, or versioned images.

## Next command

`git push -u origin agent/v0.1.1-launch-evidence`
